<?xml version="1.0" encoding="utf-8"?>
<problem
    package="unix.socket"
    id="logger"
    type="standard">
  <statement language="ru_RU">
    <description>
        <p>Напишите логгер, принимающий на заданном порту датаграммы UDP/IPv6 с произвольных адресов
            и печатающий адрес отправителя и содержимое датаграммы на stdout. Гарантируется,
            что содержимое каждой датаграммы не длиннее 1024 байт.</p>
        <p>Логгер запускается так:</p>
        <pre>./logger UDP_PORT</pre>
        <p>Получайте датаграммы с помощью <code>recvfrom</code>. Каждую пришедшую датаграмму
            следует распечатать на stdout в следующем формате: <code>[SRCADDR:SRCPORT] CONTENTS\n</code>
            (после квадратных скобок один пробел, после сообщения перевод строки). Таким образом, если отправить логгеру датаграмму
            следующим образом:</p>
        <pre>echo -n "Hi guys" | nc -u ::1 $UDP_PORT</pre>
        <p>то логгер напечатает на stdout что-то вроде</p>
        <pre>[::1:28117] Hi guys</pre>
        <p>При получении сигнала SIGTERM логгер должен завершиться с кодом 0.</p>
        <p>Вывод логгера перенаправлен в канал (учитывайте буферизацию).</p>
    </description>
  </statement>
</problem>
